- type: custom:vertical-stack-in-card
  cards:
    - type: custom:mushroom-template-card
      primary: "{{ state_attr('sensor.non_ecole', 'friendly_name') }}"
      secondary: ""
      icon: mdi:school-outline
      fill_container: true
      tap_action:
        action: none
      hold_action:
        action: none
      double_tap_action:
        action: none
      layout: vertical
      icon_color: |-
        {% if (states["sensor.non_ecole"].state == 'Ouverte') -%} 
          green 
        {%- else -%} 
          red 
        {%- endif %}
    - type: custom:button-card
      entity: sensor.non_ecole
      name: École Val-Mauricie
      show_entity_picture: true
      show_icon: false
      show_name: false
      size: 100
      state:
        - value: Ouverte
          entity_picture: /local/images/tableau_ecole_ouverte.png
        - value: Fermée
          entity_picture: /local/images/tableau_ecole_fermee.png
      tap_action:
        action: none
      hold_action:
        action: none
      double_tap_action:
        action: none
    - type: markdown
      content: >-
        ## {{ states('sensor.non_ecole') }}


        {% if state_attr('sensor.non_ecole', 'message') | trim

        != '' or state_attr('sensor.non_ecole', 'information') | trim !=
        ''

        -%}


        {% if state_attr('sensor.non_ecole', 'message') -%}
           ### Informations<br>
         * {{ state_attr('sensor.non_ecole', 'message') }}
        {% endif -%}


        {% for name in state_attr('sensor.non_ecole',

        'information')-%}
          * {{ name }}
        {% endfor -%}


        {% endif -%}
